pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'pwd'
                }
            }
        }
        
        // Add more stages here if needed
        
}

        // Remove the Docker images
        //sh 'docker-compose rm -f'
    }
}

